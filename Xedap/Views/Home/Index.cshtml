@model IEnumerable<Xedap.Models.Product>

@{
    ViewBag.Title = "Home Page";
}

<h3>Xe đạp đường phố</h3>
<div class="row">
    @foreach (var item in ViewBag.DuongPho)
    {
        <div class="col-3">
            <div class="card">
                <img src="~/Content/Image/@item.ImageURL" class="card-img-top" alt="@item.Name">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>

                    <a href="#" class="btn btn-primary">Go somewhere</a>
                    <a href="#" class="btn btn-outline-danger">Go somewhere</a>

                </div>
            </div>
        </div>
    }
</div>
<h3>Xe đạp đường phố</h3>
<div class="row">
    @foreach (var item in ViewBag.DuongPho)
    {
        <div class="col-3">
            <div class="card">
                <img src="~/Content/Image/@item.ImageURL" class="card-img-top" alt="@item.Name">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>

                    <a href="#" class="btn btn-primary">Go somewhere</a>
                    <a href="#" class="btn btn-outline-danger">Go somewhere</a>

                </div>
            </div>
        </div>
    }
</div>
<h3>Xe đạp đường phố</h3>
<div class="row">
    @foreach (var item in ViewBag.DuongPho)
    {
        <div class="col-3">
            <div class="card">
                <img src="~/Content/Image/@item.ImageURL" class="card-img-top" alt="@item.Name">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>

                    <a href="#" class="btn btn-primary">Go somewhere</a>
                    <a href="#" class="btn btn-outline-danger">Go somewhere</a>

                </div>
            </div>
        </div>
    }
</div>